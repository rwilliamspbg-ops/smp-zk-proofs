# smp-zk-proofs
robust repository containing the cryptographic primitives and tooling necessary for generating and verifying Zero-Knowledge Proofs (ZKPs) that cryptographically attest to the integrity of local data processing (e.g., model weight updates or location validation) without revealing the underlying raw inputs.
